import { User, Swords, Users, PlusCircle, Coins } from 'lucide-react';

import { useAppSelector } from "../app/hooks"


export const Navbar = ()=>{
    const {profile} = useAppSelector((state:any) => state.auth);

        return(
            <>
         
            </>
      
        )
    }
